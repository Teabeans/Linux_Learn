From the project root directory, use the command:

$ g++ -std=c++11 *.cpp <OptionalOutputName>

If G++ is not installed, it may be installed using the command

$ sudo apt install g++

After installation, check for updates and upgrades with the commands

$ sudo apt-get update
$ sudo apt-get upgrade
